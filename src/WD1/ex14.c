#include <stdio.h>

int main()
{
    float a = 10;
    float b = 15;

    printf("a + b =\t%.0f\n" , a + b);
    printf("a - b =\t%.0f\n" , a - b);
    printf("a * b =\t%.0f\n" , a * b);
    printf("a / b =\t%f\n" , a / b);

    return 0;
}