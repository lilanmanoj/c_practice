#include <stdio.h>

int main() {
    const int a = 10, b = 15;

    printf("a + b = %d\n" , a + b);
    printf("a - b = %d\n" , a - b);
    printf("a * b = %d\n" , a * b);
    printf("a / b = %f\n" , (float) a / b);

    return 0;
}