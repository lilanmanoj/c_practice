#include <stdio.h>

int main() {
    const float a = 11.2, b = 2.2;

    printf("a + b = %.2f\n" , a + b);
    printf("a - b = %.2f\n" , a - b);
    printf("a x b = %.2f\n" , a * b);
    printf("a / b = %.2f\n" , a / b);

    return 0;
}