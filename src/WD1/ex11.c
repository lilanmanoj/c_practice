#include <stdio.h>

int main()
{
    float a = 11.2;
    float b = 2.2;

    printf("a + b =\t%.2f\n" , a + b);
    printf("a - b =\t%.2f\n" , a - b);
    printf("a x b =\t%.2f\n" , a * b);
    printf("a / b =\t%.2f\n" , a / b);

    return 0;
}