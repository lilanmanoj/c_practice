#include <stdio.h>

int main() {
    // Added an additional new line character at the end
    // To prevent next prompt print on same line as result line
    printf ("This is a message with ‘single quotes’ , “double quotes” and question mark (\?).\a\n");

    return 0;
}